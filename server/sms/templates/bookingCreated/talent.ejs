Hey <%=data.contact.firstName%>, we a have a gig for you at <%=new Date(data.booking.fulfillmentDate).toLocaleTimeString('en-us')%> on <%=new Date(data.booking.fulfillmentDate).toLocaleDateString('en-us', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric'}).slice(0, -6) %>! Reply ACCEPT with the offer number: <%=data.booking.offer_id%> to book this gig. Checkout http://localhost:5000/api/v1/booking?offer=<%=data.booking.offer_id%> for more details.
