Hey <%=data.contact.firstName%>, we a have a gig for you at <%=new Date(data.booking.deliveryDateTime).toLocaleTimeString('en-us', {hour: '2-digit', minute: '2-digit', timeZone: 'America/New_York'})%> on <%=new Date(data.booking.deliveryDateTime).toLocaleDateString('en-us', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric', timeZone: 'America/New_York'}).slice(0, -6) %>! Reply ACCEPT with the offer number <%=data.booking.offer_id%> to book this gig. Checkout http://localhost:5000/api/v1/bookings?offer_id=<%=data.booking.offer_id%> for more details.